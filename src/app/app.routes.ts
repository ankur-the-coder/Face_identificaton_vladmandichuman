import { Routes } from '@angular/router';
import { FaceDetectorComponent } from './components/face-detector/face-detector.component';

export const routes: Routes = [
    { path: '', component: FaceDetectorComponent }
];
